docker {
  enabled = true
}

process {
  container = 'serology-imputation:latest'
  containerOptions = '--entrypoint=""'
}

process {
  withName: impute {
    cpus = 1
    memory = '2 GB'
    maxForks = 4
  }
}

executor {
  queueSize = 4
}
